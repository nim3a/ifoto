# Simple runtime-only image using pre-built dist
FROM nginx:alpine

# Copy pre-built files (Angular 17 outputs to browser subdirectory)
COPY dist-copy/ifoto-frontend/browser /usr/share/nginx/html

# Copy nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
