# PostgreSQL Configuration
POSTGRES_DB=ifoto
POSTGRES_USER=ifoto
POSTGRES_PASSWORD=change_this_secure_password

# JWT Configuration
JWT_SECRET=change_this_to_a_very_long_random_string_at_least_64_characters_for_production

# Minio Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_this_secure_password
MINIO_BUCKET=ifoto

# Backend Configuration
SERVER_PORT=8080
STORAGE_TYPE=minio

# Qdrant Configuration
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION=face_embeddings

# Face Service Configuration
FACE_SERVICE_URL=http://face-service:5000

# Frontend Configuration (if needed)
API_URL=http://localhost:8080
