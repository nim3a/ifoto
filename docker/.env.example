# PostgreSQL Configuration
POSTGRES_DB=ifoto
POSTGRES_USER=ifoto
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_STRONG_SECURE_PASSWORD_FOR_PRODUCTION

# JWT Configuration (MUST be at least 64 characters for security)
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_STRING_AT_LEAST_64_CHARACTERS_FOR_PRODUCTION_USE_ONLY

# Minio Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=CHANGE_THIS_TO_A_STRONG_SECURE_PASSWORD_FOR_PRODUCTION
MINIO_BUCKET=ifoto

# Backend Configuration
SERVER_PORT=8080
STORAGE_TYPE=minio

# Qdrant Configuration
QDRANT_HOST=qdrant
QDRANT_PORT=6333
QDRANT_COLLECTION=face_embeddings

# Face Service Configuration
FACE_SERVICE_URL=http://face-service:5000

# Frontend Configuration (if needed)
API_URL=http://localhost:8080
